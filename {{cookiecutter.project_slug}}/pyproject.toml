[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{ cookiecutter.project_slug }}"
version = "0.1.0"
description = "{{ cookiecutter.project_description }}"
authors = [{name = "{{ cookiecutter.author_name }}", email = "{{ cookiecutter.author_email }}"}]
readme = "README.md"
license = {text = "{{ cookiecutter.license }}"}
requires-python = ">={{ cookiecutter.python_version }}"
keywords = []
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: {{ cookiecutter.license }} License",
    "Programming Language :: Python :: {{ cookiecutter.python_version }}",
    "Typing :: Typed",
]

dependencies = [
{%- if cookiecutter.use_pydantic %}
    "pydantic~=2.13",
    "pydantic-settings~=2.14",
{%- endif %}
{%- if cookiecutter.use_fastapi %}
    "fastapi[standard]~=0.141",
    "uvicorn~=0.52",
{%- endif %}
{%- if cookiecutter.use_database %}
    "sqlalchemy~=2.0",
    "sqlmodel~=0.0.39",
    "alembic~=1.19",
    "asyncpg~=0.31",
{%- endif %}
{%- if cookiecutter.use_cli %}
    "typer~=0.27",
    "rich~=15.0",
{%- endif %}
]

[dependency-groups]
lint = [
    "bandit~=1.9",
    "commitizen~=4.17",
    "pip-audit~=2.10",
{%- if cookiecutter.hooks_runner == "prek" %}
    "prek~=0.4.12",
{%- else %}
    "pre-commit~=4.6",
{%- endif %}
    "ruff~=0.16",
    "yamllint~=1.38",
]

type = [
    "mypy~=2.3",
    "pyright~=1.1.411",
    "ty~=0.0.68",
    "pyrefly~=1.2",
    "types-pyyaml~=6.0.12",
]

test = [
    "coverage~=7.15",
    "hypothesis>=6.165",
    "pytest~=9.1",
    "pytest-asyncio~=1.4",
    "pytest-benchmark~=5.2",
    "pytest-cov~=7.1",
    "pytest-memray>=1.9; sys_platform != 'win32'",
    "pytest-mock~=3.15",
    "pytest-xdist~=3.8",
    "testcontainers>=4.15",
]
{% if cookiecutter.use_jupyter_book %}

docs = [
    "jupyter-book~=2.1",
]
{% endif %}

dev = [
    {include-group = "lint"},
    {include-group = "type"},
    {include-group = "test"},
{%- if cookiecutter.use_jupyter_book %}
    {include-group = "docs"},
{%- endif %}
]

{% if cookiecutter.use_cli  %}
[project.scripts]
{{ cookiecutter.project_slug }} = "{{ cookiecutter.package_name }}.cli:app"
{% endif %}

[project.urls]
Homepage = "https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.project_slug }}"
Repository = "https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.project_slug }}"
Issues = "https://github.com/{{ cookiecutter.github_username }}/{{ cookiecutter.project_slug }}/issues"

[tool.hatch.build.targets.wheel]
packages = ["{{ cookiecutter.package_name }}"]

[tool.uv]
managed = true

[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "pep621"
tag_format = "v$version"
changelog_incremental = true

[tool.bandit]
exclude_dirs = [".eggs", ".git", ".hg", ".mypy_cache", ".tox", "venv", ".venv", "build", "dist"]

# Note: pytest, ty, and pyrefly configurations are in separate files:
# - .pytest.ini
# - ty.toml
# - pyrefly.toml
